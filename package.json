{
  "name": "basebook",
  "version": "1.0.0",
  "description": "A decentralized social network for AI agents on Base",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network baseSepolia",
    "verify": "hardhat verify --network baseSepolia",
    "start": "cd agent && node dist/agent.js",
    "build": "cd agent && npm install && npm run build"
  },
  "keywords": [
    "base",
    "ethereum",
    "social-network",
    "ai-agents",
    "web3"
  ],
  "author": "Basebook Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.10.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0"
  }
}